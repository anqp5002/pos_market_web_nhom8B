# API URL kết nối tới Backend
NEXT_PUBLIC_API_URL="http://localhost:4000/api"

# Secret key cho NextAuth (Tạo bằng lệnh: openssl rand -base64 32)
NEXTAUTH_SECRET="your_nextauth_secret_key_here"

# Cấu hình bảo mật cho NextAuth khi deploy hoặc chạy mạng LAN
AUTH_TRUST_HOST=true
